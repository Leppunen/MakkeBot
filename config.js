const credentials = require('./credentials');

module.exports = {
    discordToken: credentials.discordToken,
    _1uApiKey: credentials._1uApiKey,
    devChannels: credentials.devChannels,
    giturl: 'https://raw.githubusercontent.com/Leppunen/MakkeBot/master/package.json',
};
